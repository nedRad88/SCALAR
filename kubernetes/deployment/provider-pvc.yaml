kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: provider-pvc
  annotations:
    volume.beta.kubernetes.io/storage-class: azure-disk
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
